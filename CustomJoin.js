var arr1 = [1,2,3,4,5,6,7];
var arr2 = [8,9,10];

var joint_array = [];

for(index = 0;index<arr1.length; ++index){
	joint_array.push(arr1[index]);
}
for(index = 0;index<arr2.length;++index){
	joint_array.push(arr2[index]);
}
for(index = 0; index<joint_array.length;  ++index){
	console.log(joint_array[i]);
}